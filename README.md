# P2P on demand video streaming based on tit-for-tat strategy

##### Dependencies:

* Gorilla Mux
    ```
    > go get -u github.com/gorilla/mux
    ```
* ffmpeg
    ```
    > brew update
    > brew install
    ```
